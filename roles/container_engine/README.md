# container_engine

Installs and configures the container runtime (such as containerd, CRI-O, or gVisor) required for Kubernetes nodes. Manages installation, configuration files, and system limits to ensure a reliable container environment.
