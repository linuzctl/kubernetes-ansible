# k8s

Handles installation and configuration of core Kubernetes components, including kubeadm, kubelet, and kubectl. Manages cluster initialization, node joining, and generates kubeadm configuration files.
