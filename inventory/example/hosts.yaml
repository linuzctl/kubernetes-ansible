---
k8s_control_plane:
  hosts:
    master1:
      # ansible_host: master1.example.com
    master2:
      # ansible_host: master2.example.com
    master3:
      # ansible_host: master3.example.com
k8s_worker:
  hosts:
    worker1:
      # ansible_host: worker1.example.com
    worker2:
      # ansible_host: worker2.example.com
    worker3:
      # ansible_host: worker3.example.com

k8s:
  children:
    k8s_control_plane:
    k8s_worker:
  # vars:
  #   ansible_connection: ssh
  #   ansible_user: example
  #   ansible_ssh_private_key_file: /path/to/ssh_key
